# POrtfolio - spectrum task-3
---
- First create a database with your name.
- Import the sql file in database from database folder.
- Run the project in browser as local app (url: localhost/<project name>).